### Network Topology

![ 500](../_assets/Pasted%20image%2020260609211354.png)

### Network Route/Host Route

![Pasted image 20260609211446.png](../_assets/Pasted%20image%2020260609211446.png)

**Network Route** - A route to a network/subnet (mask length \< /32)
**Host Route** - A route to a specific host (/32 mask)

### Dynamic Routing

![ 500](../_assets/Pasted%20image%2020260609211939.png)
![Pasted image 20260609212005.png](../_assets/Pasted%20image%2020260609212005.png)
![Pasted image 20260609212112.png](../_assets/Pasted%20image%2020260609212112.png)

We can see that once routers receive the advertised path, they will add it to routing table. Notice in R1 that it also learned the path to `10.0.24.0/30` through R2!

What if the connection between R2 and R4 got cut?

![Pasted image 20260609212407.png](../_assets/Pasted%20image%2020260609212407.png)

The routers will adapt and remove the route. In this example, R1 removed all the routes that passes through R4.

![Pasted image 20260609212534.png](../_assets/Pasted%20image%2020260609212534.png)

What if we configured **static routing** instead? Because there is no dynamic routing, R1 will still try to reach the dead end because it will be unaware that the link in R4 is not functioning.

But we always *should* have a **backup route**. So instead of removing a destination network from the routing table, we should map it to the **next best route** to reach the destination network.

![Pasted image 20260610043021.png](../_assets/Pasted%20image%2020260610043021.png)

Let's add new route from R3 to R4. Now we should have 2 valid networks for R1 to reach R4.

![Pasted image 20260610043330.png](../_assets/Pasted%20image%2020260610043330.png)

We can now reach them via R2 or via R3. We can see in the routing table that we can reach the VLAN network (`192.168.4.0/24`) through R2 (`10.0.12.2/30`). Let's say that R4's `G0/0` interface got cut or disabled:

![Pasted image 20260610043600.png](../_assets/Pasted%20image%2020260610043600.png)

We lost the best path through `10.0.12.2`, but we can still reach them through the next best path, which is `10.0.13.2` to reach the VLAN. You can see in the routing table that the path to `192.168.4.0/24` was not removed but replaced with the next best path.

**But why the route to R2 is preferred and not via R3?**

![ 500](../_assets/Pasted%20image%2020260610043808.png)
![ 500](../_assets/Pasted%20image%2020260610043820.png)

This is because the route via R3 uses **FastEthernet** but the route via R2 uses **GigabitEthernet**!

* In STP, root cost let us determine the best path to the root bridge and we also know that the better the speed of an interface, the lower the root cost. Dynamic routing is similar in such a way that it prefers interfaces with higher speed to determine the best path to the network.

---

### Dynamic Routing Summary

![ 500](../_assets/Pasted%20image%2020260610044325.png)

* Routers can use dynamic routing protocols to advertise information about the routes they know to other routers.
* They form 'adjacencies' / 'neighbor relationships' / 'neighborships' with adjacent routers to exchange this information. In the example topology above, R1 will form adjacencies with R2 and R3, it's directly connected neighbors.
* If multiple routes to a destination are learned, the router determines which route is superior and adds it to the routing table. It sues the 'metric' of the route to decide which is superior (lower metric = superior). So, just like with STP, a lower root cost is the more superior switch!

---

### Dynamic Routing Protocol Types

**IGP (Interior Gateway Protocol** - used to share routes within a single *autonomous* system (AS), which is a single organization (ie. a company).

**EGP (Exterior Gateway Protocol** - used to share routes *between* different autonomous systems.

![Pasted image 20260610044718.png](../_assets/Pasted%20image%2020260610044718.png)

In the example topology above, **Company A**, **ISP A**, **ISP B**, and **Company B** are each their own autonomous system. The connected nodes/routers inside a **red circle** is a single autonomous system (AS) so it uses **IGP**. The links highlighted in **purple** is used to connect theses autonomous systems (AS's) together to exchange routing information which uses **EGP**.

---

### IGP/EGP Algorithm Types

![Pasted image 20260610045557.png](../_assets/Pasted%20image%2020260610045557.png)

For the **CCNA**, we will mainly focus on **OSPF** (Often Shortest Path First) which uses a **Link State** algorithm.

---

### Distance Vector Routing Protocols

* Distance vector protocols were invented *before* link state protocols.
* Early examples are **RIPv1** and Cisco's proprietary protocol **IGRP** (which was updated to **EIGRP**).
* Distance vector protocols operate by sending the following to their directly connected neighbors:
  - their known destination networks
  - their metric to reach their known destination networks
* This method of sharing route information is often called **routing by rumor**. This is because the router doesn't know about the network beyond its neighbors. It only knows the information that its neighbors tell it.
* This algorithm is called 'distance vector' because the routers only learn the **distance** (metric) and **vector** (direction, the next-hop router) of each route.

![ 500](../_assets/Pasted%20image%2020260610050334.png)

The example above uses distance vector algorithm to calculate the shortest path. The routing information are advertised to the neighbor routers but the routers will not know the network beyond what it knows from the neighboring/adjacent routers.

---

### Link State Routing Protocols

* When using a **link state** routing protocol, every router creates a 'connectivity map' of the network.
* To allow this, each router advertises information about its interfaces (connected networks) to its neighbors. These advertisements are passed along to other routers, until all routers in the network develop the same map of the network.
* Each router independently uses this map to calculate the best routes to each destination.
* Link state protocols use *more resources* (CPU) on the router,  because more information is shared.
* However, link state protocols tend to be faster in reacting to changes in the network than distance vector protocols.

---

### Metric

* A router's route table contains the best route to each destination network it knows about.
* If a router using a dynamic routing protocol learns two different routes to the same destination, how does it determine which is **best**?
* It uses the **metric** value of the routers to determine which is best. A lower metric = better.
* Each routing protocol uses a different metric to determine which route is the best.

![Pasted image 20260610051331.png](../_assets/Pasted%20image%2020260610051331.png)
![Pasted image 20260610051408.png](../_assets/Pasted%20image%2020260610051408.png)

* You can see that both paths are added to the routing table because both uses GigabitEthernet connection!
* If a router learns two (or more) routes via the **same routing protocol** to the **same destination** (same network address, same subnet mask) with the **same metric**, both will be added to the routing table. Traffic will be load-balanced over both routes.

![Pasted image 20260610051648.png](../_assets/Pasted%20image%2020260610051648.png)

* You can see that the paths are learned through **OSPF** because the connected routes uses **O** flag.
* The 2 paths to the same destination (`192.168.4.0/24`) are added because both have the same **metrics**.
* The numbers inside the **orange line box** is the **metric value** of the route. Since both paths have the same value, both are added.
* Since both paths have the same metrics that lead to the same destination, the traffic will be load-balanced to reach the destination. This is also known as **Equal Cost Multi-Path** (ECMP).

![Pasted image 20260610052124.png](../_assets/Pasted%20image%2020260610052124.png)

* Another thing to remember is the values inside the **aquamarine line box**. This is called **Administrative Distance** (AD).
* The OSPF Protocol has an AD of **110**.

---

### ECMP with Static Routes

![Pasted image 20260610054142.png](../_assets/Pasted%20image%2020260610054142.png)

* You can also achieve ECMP with static routes.
* By disabling the OSPF in R1, you can manually add them using `ip route` command. Just add the 2 routes on the same distance (one via R2, and one via R3).
* Both routes will then be added to the routing table, and traffic will be load-balanced.

![Pasted image 20260610054333.png](../_assets/Pasted%20image%2020260610054333.png)

* Static routes do not have the concept of metrics, so its value will be **0**.
* The **AD** value of static routes is **1**.

---

### IGP Metric Chart

![Pasted image 20260610054629.png](../_assets/Pasted%20image%2020260610054629.png)
![ 500](../_assets/Pasted%20image%2020260610055112.png)

* Since RIP only use hop counts, both paths (even though one uses a slower bandwidth) will be valid to reach the destination. This is because both paths will have a hop count of 2 (R1 -> **R2** -> **R4** -> `192.168.4.0/24` and R1 -> **R3** -> **R4** -> `192.168.4.0/24`)
* OSPF will only consider the path via R2 since it uses a faster bandwidth (GigabitEthernet over FastEthernet).

---

### Administrative Distance

* In most cases a company will only use a single IGP - usually OSPF or EIGRP.
* However, in some rare cases they might use two. For example, if two comanies connect their networks to share information, two different routing protocols might be in use.
* Metri is used to compare routes <u>learned via the same routing protocol</u>.
* Different routing protocols use totally different metrics, so they cannot be compared.
* For example, an OSPF route to `192.168.4.0/24` might have a metric of 30, while an EIGRP route to the same destination might have a metric of 33280. Which route is better? which route should the router put in the route table?
* The **administrative distance** (AD) is used to determine which routing protocol is preferred. A *lower* AD is preferred, and indicates that the routing protocol is considered more 'trustworthy' (more likely to select good routes).

![ 250](../_assets/Pasted%20image%2020260610055953.png)![ 250](../_assets/Pasted%20image%2020260610060019.png)

Keep in mind that the **AD** values in the table are specifically for *Cisco devices*. Other vendors might use different values. For Cisco, ***If the administrative distance is 255, the router does not believe the source of that route and does not install the route in the routing table***.

![Pasted image 20260610060608.png](../_assets/Pasted%20image%2020260610060608.png)

---

### Configure AD of a Static Route

* You can change the AD of a routing protocol (we will learn this later in OSPF).

* You can also change the AD of a static route:
  ![ 500](../_assets/Pasted%20image%2020260610060815.png)
  
  Keep in mind that it has the word *metric*, but this value is actually for **AD** so you won't be confused.
  
  ![ 500](../_assets/Pasted%20image%2020260610060927.png)
  
  In the example above, we configured the AD value of a static route to **100**. You can also see that the new AD value for the static route is reflected in the routing table.

---

## Floating Static Routes

* By changing the AD of a static route, you can make it less preferred than routes learned by a dynamic routing protocol to the same destination (make sure the AD is higher than the routing protocol's AD!)
* This is called a **floating static route**.
* The route will be inactive (not in the routing table) unless the route learned by the dynamic routing protocol is removed (for example, the remote router stops advertising it for some reason, or an interface failure causes and adjacency with a neighbor to be lost).

---

### Things we covered

![ 500](../_assets/Pasted%20image%2020260610061549.png)
