**Loop Guard** - protects the network from loops by disabling a port if it unexpectedly stops receiving BPDUs, ensuring it does not mistakenly enter the Forwarding state.

---

### Unidirectional Links

![Pasted image 20260604090303.png](../_assets/Pasted%20image%2020260604090303.png)

* Cables/fiber optics should be **bidirectional** in nature, where both ends of the cable can transmit and receive data.
* A **unidirectional link** is a network link where data transmission occurs in only one direction. For example, SW1 can send frames to SW2, but SW2 can't send frames to SW1.
* Unidirectional links are typically caused by Layer 1 (Physical) issues.
  * Damaged cables
  * Faulty connectors or transceivers (e.g. SFP modules)

![Pasted image 20260604090606.png](../_assets/Pasted%20image%2020260604090606.png)

* Unidirectional links are more common with fiber-optic cables than copper UTP.
  * Fiber optic connections typically use two separate fibers.
    * If one fiber is damaged, it can disrupt data flow in one direction while the other remains unaffected.
  * Fiber optics are more vulnerable to physical damage than copper UTP cables.

![Pasted image 20260604090818.png](../_assets/Pasted%20image%2020260604090818.png)

* For a fiber-optic interface to be **up/up**, both fibers must be connected and functional.
* If there is a physical problem with either fiber, the devices **should** be able to detect it and disable their interfaces.
* If the devices fail to detect the physical problem, it could result in a **unidirectional link**.

![Pasted image 20260604091011.png](../_assets/Pasted%20image%2020260604091011.png)

* So, in unidirectional link, both devices think they are perfectly connected, but is actually only one-way.

---

### Loop Guard: The Problem

![Pasted image 20260604091407.png](../_assets/Pasted%20image%2020260604091407.png)

* BPDUs originate from the Root bridge and are forwarded out of Designated ports every 2 seconds.
* SW3 G0/1 is a **Non-Designated** blocking port because it receives superior BPDUs from SW2 (i.e. SW2 has a superior root cost or bridge ID)

![Pasted image 20260604091530.png](../_assets/Pasted%20image%2020260604091530.png)

* If the SW2-SW3 link becomes unidirectional and SW2's BPDUs can't reach SW3, what will happen?

![Pasted image 20260604091806.png](../_assets/Pasted%20image%2020260604091806.png)

* In this case, once the **Max Age timer** of the STP inside SW3 runs out (which is typically after 20 seconds), SW3 will review its topology once again and set its G0/1 port to a **Designated** forwarding port and start forwarding BPDUs.
* Because SW3's BPDUs are inferior to SW2's, SW2 simply ignores SW3's BPDUs.
  * SW2 G0/1 and SW3 G0/1 will **both** be in the forwarding state. This will cause **SW1-SW3-SW2** loop!

![Pasted image 20260604092131.png](../_assets/Pasted%20image%2020260604092131.png)

Now you understand why unidirectional links are a problem!

---

### Loop Guard: The Solution

As a disclaimer, loop guard DO NOT prevent physical unidirectional links, but otherwise provide mechanism to DETECT unidirectional links and prevent Layer 2 loops.

![Pasted image 20260604092536.png](../_assets/Pasted%20image%2020260604092536.png)

* When a **Loop Guard**-enabled port's Max Age timer counts down to 0, it doesn't become a Designated port and start transitioning to forwarding.
  * It enters the **Broken** (*Loop Inconsistent*) state.
  * Like the **Broken** (*Root Inconsisten*) state triggered by a Root Guard violation, this blocks the port.
    * **NOTE:** *In both cases, the port remains **up/up**, but STP blocks it.*

![Pasted image 20260604092903.png](../_assets/Pasted%20image%2020260604092903.png)

* If the broken port starts receiving BPDUs again, it will be *automatically re-enabled*.
* Loop Guard can be enabled in two ways:
  * Per-port: `SW3(config-if)# spanning-tree guard loop`
  * Default: `SW3(config)# spanning-tree loopguard default`
    * This enables Loop Guard on all ports
    * Use `SW3(config-if)# spanning-tree guard none` to disable it on specific ports if needed.
* **Loop Guard** should be enabled on **Root** and **Non-Designated** ports (ports that are supposed to *receive* BPDUs).

---

### Loop Guard: CLI Demonstration

![Pasted image 20260604093217.png](../_assets/Pasted%20image%2020260604093217.png)

Let's enable it per port first:

![Pasted image 20260604093242.png](../_assets/Pasted%20image%2020260604093242.png)

To enable it in global config mode:

![Pasted image 20260604093332.png](../_assets/Pasted%20image%2020260604093332.png)

What happens when Loop Guard block a port?

![Pasted image 20260604093413.png](../_assets/Pasted%20image%2020260604093413.png)
![Pasted image 20260604093515.png](../_assets/Pasted%20image%2020260604093515.png)

Let's say that we replace the old broken cable to prevent unidirectional link:

![Pasted image 20260604093620.png](../_assets/Pasted%20image%2020260604093620.png)
![Pasted image 20260604093631.png](../_assets/Pasted%20image%2020260604093631.png)

The Loop Guard automatically unblocks the port once it receives another BPDU!

---

### Loop Guard & Root Guard

![Pasted image 20260604094030.png](../_assets/Pasted%20image%2020260604094030.png)

* **Loop Guard** and **Root Guard** are mutually exclusive.
  * ***They can't be enabled on the same port at the same time.***
  * **Root Guard** is meant to prevent *Designated ports* from becoming *Root ports*.
  * **Loop Guard** is meant to prevent *Non-Designated or Root ports* from becoming *Designated ports*.
* If Loop Guard is configured on a port (`spanning-tree guard loop`) and youthen configure Root Guard (`spanning-tree guard root`), Loop Guard will be disabled on the port
  * **and vice-versa**
* If Loop Guard is enabled by default (`spanning-tree loopguard default`) and then you configure Root Guard on a port, Loop Guard will be disabled on the port.
  * This is because the *more specific configuration* (interface vs global) takes effect.
  * The more specific configuration effect is also true in other protocols. For example, in PortFast, if you enable it on all access ports on default (`spanning-tree portfast default`), applying `spanning-tree portfast disable` on a specific access port will take precedence and disable PortFast on that specific interface.

---

### Summary

![Pasted image 20260604095041.png](../_assets/Pasted%20image%2020260604095041.png)
